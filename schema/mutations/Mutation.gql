type Mutation {
  addSpecies(species: SpeciesInput!): Species
  addEntry(entry: EntryInput!): Entry
  addSite(site: SiteInput!): Site
  createToken(username: String!, password: String!): Result!
}